# LC 8 — String to Integer (atoi)
- **Pattern:** Math/parsing — linear scan: trim, parse optional sign, skip leading zeros, accumulate digits with `checked { }` overflow guard (clamp to int.MaxValue/MinValue)
- **Brute force:**
- **Optimized:**
- **Key insight:**
- **Edge cases I had to handle:**
- **Where I got stuck and for how long:**
- **Template fragments I reused:**
- **Would I solve this in 25 min cold next week? Y/N**
