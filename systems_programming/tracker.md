# Systems Programming — Study Tracker

**Goal:** build **single-machine concurrency in C# from scratch** to an **L5 bar** for the Databricks **Systems Programming** round. 12-lesson curriculum ([study/00-index.md](study/00-index.md)), driven by [../full_loop/study-plan.md](../full_loop/study-plan.md) (§2, §5). This is the **~60% effort** track — the one true gap.

**Window:** start **Tue 2026-07-14** → concurrency lessons land across **Weeks 1–3** (Jul 14 – Aug 3), mocks Week 4 (Aug 4–6), **loop Fri 2026-08-07**. Pace is **not** one-a-day; it's *lessons interleaved with algo upkeep, coding-round drills, behavioral, and weekend builds/mocks* per the week-by-week plan. Checkpoint every **Sunday** — if concurrency comprehension lags, extend the date rather than cram.

Each lesson: **written to full depth (sourced) → read → run the demo → Self-check**; next session opens with an **open-ended quiz on the prior lesson** (spaced recall). Grades → [grades/](grades).

**Lesson states:** `planned` → `written` (drafted to depth, sourced) → `studied` (read + demo run + self-check) → `quizzed ✓` (recalled next session).

Date format: `YYYY-MM-DD`.

---

## Week plan (from study-plan.md §5)

| Week | Dates | Lessons | Other tracks running alongside |
|------|-------|---------|-------------------------------|
| **W1** | Jul 14 – Jul 20 | **01–04** (threads, races, locks, memory model) | draft 6–8 STAR stories · algo (4 warm-up rounds + 4 cold) · math: bit manip · 2 coding-round builds |
| **W2** | Jul 21 – Jul 27 | 05–08 (condition sync, primitives, concurrent structures, pools) | algo upkeep · math: number theory · 2 coding builds · behavioral rehearse · Mock #1 |
| **W3** | Jul 28 – Aug 3 | 09–12 (thread-safe cache ⭐, durability, I/O, rate limiter) | algo upkeep · math: combinatorics/prob · 2 coding builds · Mock #2 + #3 |
| **W4** | Aug 4 – Aug 6 | mocks + patch weak spots + taper | 2 full mock loops · algo (recycle) · Thu = light taper |

---

## Lesson progress

| # | Lesson | Ships (C# demo) | Written | Studied | Quizzed | Notes |
|---|--------|-----------------|---------|---------|---------|-------|
| 01 | Threads & execution model (process vs thread, Thread/Task/pool, scheduling, context switch, concurrency vs parallelism, why concurrency) | `01-threads` | ✅ v1.0 (07-14) | ✅ 07-14 | ✅ 07-15 **Strong-to-Solid** (4 Strong/5 Partial/0 Weak) | Taught 07-14; quizzed 07-15 (10 Qs → [grades](grades/01-threads-execution-model.md)). Re-drill: race≠deadlock, locals-private-via-stack, 3 costs+one-liner, ManagedThreadId consequence, latency-ladder units+payoff. |
| 02 | Shared state & race conditions | `02-races` | ✅ v1.0 (07-15) | ✅ 07-15 (demo run) | ✅ 07-16 **Solid** (5 Strong/5 Partial/0 Weak) | Written to full depth, sourced (MS Learn race-conditions [S1]; C# spec §9.6 atomicity [S2]). First lesson in the **new reframe**: concept-first, pseudocode primitives, 4 race-shapes + fix-landscape tradeoffs, diagrams. Demo `02-races`: 20/20 racy runs lost updates, 20/20 atomic runs exact. Quizzed 07-16 (10 Qs, integrative w/ L01 → [grades](grades/02-shared-state-races.md)). Re-drill: precise critical-section def, SILENT corruption as headline danger, confinement=often-best fix, lock granularity trap, block-on-lock→2 context switches. |
| 03 | Mutual exclusion & locks (+ deadlock) | `03-locks` | ✅ v1.0 (07-16) | ✅ 07-16 (demo run) | ✅ 07-19 **Solid** (5 Strong/5 Partial/0 Weak) | Written to full depth, sourced ([S1] MS Learn `lock` statement, [S2] sync primitives overview, [S3] Coffman deadlock conditions). Covers: lock = mutual exclusion (acquire/release, safety+liveness, reentrancy), cost model (blocking=deschedule/2 context switches vs spinlock=busy-wait), granularity (coarse vs striped), 5 lock design rules (SRP/private lock/hold briefly/no foreign calls/lock reads), deadlock + 4 Coffman conditions, lock ordering + TryEnter + coarsen, livelock/starvation. Demo `03-locks`: locked counter 20/20 exact; opposite-order deadlock detected via watchdog, consistent-order completes. **Taught 07-16.** Quizzed 07-19 (10 Qs → [grades](grades/03-mutual-exclusion-locks.md)). Re-drill: safety/liveness defs, deadlock≠contention, starvation, cache stampede/single-flight. |
| 04 | Memory model & visibility (volatile, barriers, Interlocked/CAS) | `04-memory` | ✅ v1.0 (07-20) | ✅ 07-20 (demo run) | ✅ 07-21 **Solid** (7 Strong/3 Partial/0 Weak) | Written to full depth, sourced ([S1] volatile keyword, [S2] Thread.MemoryBarrier, [S3] Interlocked class, [S4] Interlocked.CompareExchange — all pulled/verified live 2026-07-20). Covers: 3 reordering/visibility layers (compiler/JIT, CPU, cache), the stop-flag-that-never-stops, memory model + **happens-before**, `volatile` (visibility+ordering of one field, NOT atomic RMW, NOT multi-field), memory barrier/fence as the primitive, **Interlocked/CAS** + the retry loop (optimistic concurrency) + ABA preview, and the unifier: **a lock = mutual exclusion + memory barrier → visibility free**. Targets the 4 L03 re-drill gaps in the opening refresher. Demo `04-memory`: plain `++` 0/20, Interlocked 20/20, **volatile-`++` 0/20** (visibility≠atomicity); volatile stop-flag stops, non-volatile under watchdog (JIT-dependent). **Taught 07-20.** Quizzed 07-21 (10 Qs → [grades](grades/04-memory-model-visibility.md)). Re-drill: 3-layer taxonomy (compiler/JIT vs CPU vs cache — don't merge), happens-before ≠ program order (must be *established* by sync), Debug disables JIT hoist (not "single-thread"), state visibility≠atomicity out loud. |
| 05 | Condition synchronization (producer–consumer, async logger) | *(skipped per scope)* | ✅ v1.0 (07-21) | ✅ 07-21 (read) | — | Written to full depth, sourced ([S1] Monitor.Wait, [S2] Monitor.Pulse, [S3] BlockingCollection\<T\>, [S4] BlockingCollection overview — all pulled/verified live 2026-07-21). Covers: busy-wait anti-pattern (→ L03 single-core disaster); the **condition variable** + 3 ops (wait/signal/broadcast); the **atomic release-and-sleep** inside `wait` + the **lost-wakeup** bug if not atomic; canonical waiter/signaler skeleton; CV **remembers nothing** → shared state is the truth; the golden rule **`while` not `if`** (Mesa semantics [S2] + spurious + broadcast); **signal vs broadcast** + thundering herd + two-CV fix; the **bounded producer–consumer** (notFull/notEmpty) + **backpressure** (unbounded → OOM); the **async logger** [S7] (one writer/many loggers, batching, poison-pill/`CompleteAdding` shutdown, durability deferred → L10); language note (Monitor + BlockingCollection\<T\>/Channel\<T\>). **No demo** (scoped out; offered on request). **Taught 07-21.** Next: quiz L05, then write L06 (higher-level primitives). |
| 06 | Higher-level primitives (Semaphore, RW-lock, Barrier…) | *(skipped per scope)* | ✅ v1.0 (07-22) | ✅ 07-22 (read) | — | Written to full depth, sourced ([S1] SemaphoreSlim, [S2] ReaderWriterLockSlim, [S3] ManualResetEvent, [S4] CountdownEvent, [S5] Barrier, [S6] Semaphore/SemaphoreSlim conceptual — all pulled/verified live 2026-07-22). Covers: the drawer of 5 primitives as crystallized lock+CV patterns; **semaphore** (permit counter, acquire/release, **binary≠mutex**: no owner/affinity, any thread releases, not reentrant, extra-release lets two in; throttle + resource pool; L05 bounded queue = 2 semaphores + mutex); **read-write lock** (readers xor writer, when it beats a plain lock = reads dominate AND long CS, **writer starvation** + favor-writers fairness, upgradeable read, →L07 striping/L09 cache, SynchronizedCache SRP); **events/latches** (manual-reset=gate wakes all/stays open vs auto-reset=turnstile wakes one/re-closes, start-signal); **countdown latch** (fork/join, one-shot, separate waiter — the L03 watchdog); **barrier** (N peers rendezvous cyclically per phase, SignalAndWait + post-phase action); decision table + "built from lock+CV" recipes. **No demo** (per L02/03/04/09 scoping — offered throttle/RW-cache on request). **Taught 07-22.** Next: quiz L06, then write L07 (concurrent data structures). |
| 07 | Concurrent data structures (striping, lock-free/CAS) | planned | — | — | — | planned |
| 08 | Thread pools & task scheduling (async/await model) | planned | — | — | — | planned |
| 09 | ⭐ Thread-safe cache (LRU/LFU + TTL, striping) | planned | — | — | — | planned — the canonical Q |
| 10 | Durability & crash-safety (WAL, fsync, "power goes down") | planned | — | — | — | planned |
| 11 | Disk & network I/O (buffering, batching, backpressure) | planned | — | — | — | planned |
| 12 | Interview integration (rate limiter, SRP/cohesion/coupling, trade-off narration) | planned | — | — | — | planned |

---

## Log

- **2026-07-14** — Track scaffolded. Created `study/00-index.md`, `study/01-threads-execution-model.md` (v1.0, full depth + Sources appendix), `tracker.md`, `grades/`. **Lesson 01 written and taught.** Verified facts live against MS Learn (threads-and-threading, managed-thread-pool, Thread class, TPL) — cited [S1]–[S4],[S7]; latency figures flagged as approximate. Demo `01-threads` added to the harness (spin up threads → observe non-deterministic interleaving + Join). **Next session:** open with a spoken quiz on Lesson 01, then write + teach Lesson 02 (shared state & races).
- **2026-07-15** — **Lesson 01 quizzed** (spaced recall, 10 open-ended Qs) → graded to [grades/01-threads-execution-model.md](grades/01-threads-execution-model.md). **Overall Strong-to-Solid: 4 Strong / 5 Partial / 0 Weak.** Strong on context-switch cost model, Thread/pool/Task mapping, background lifecycle, demo mental model; deep engagement (challenged framings, sharp await-vs-block & Join-vs-liveness follow-ups). Partial gaps to re-touch: race≠deadlock; locals-private-because-of-per-thread-stack (not allocation timing); the 3 costs + senior one-liner (+ "concurrency only speeds up work that *waits*"); ManagedThreadId = managed-only identity, no OS assumptions; latency-ladder unit slips + the "waiting thread steps aside" payoff. Recurring pattern confirmed (matches system_design grades): correct conclusion fast, under-explains the consequence half. **Next: write + teach Lesson 02 (shared state & races).**
- **2026-07-15** — **Teaching REFRAME adopted** (see repo memory `systems-programming-study-style.md`; persisted into `study/00-index.md` + `../full_loop/study-plan.md`). Round = pseudocode-in-room (Alex email), graded on reliable/fast/scalable + OO design (SRP/cohesion/coupling). → Lessons now concept-first, primitives in generic pseudocode, design/tradeoff framing, C# demoted to optional watch-it-happen harness (L02/03/04/09 only). Individual lesson rows in index + study-plan re-aligned to generic primitive names.
- **2026-07-15** — **Lesson 02 written + taught** (first lesson under the reframe). `study/02-shared-state-races.md` v1.0, full depth + Sources appendix ([S1] MS Learn race conditions, [S2] C# spec §9.6 atomicity). Covers: shared+mutable+concurrent triple, `count++` lost-update walkthrough (with interleaving diagram), critical section / atomicity / interleaving, non-determinism & Heisenbugs, the **4 race shapes** (RMW, check-then-act, torn read, multi-var invariant), and the **fix landscape** (mutual exclusion / atomic-CAS / immutability / confinement) with a tradeoffs table. Demo `02-races` added to harness (uses existing `SurfaceRaces` helper): **20/20 racy runs lost updates, 20/20 atomic runs exact.** **Next session:** quiz Lesson 02 (spaced recall), then write + teach Lesson 03 (mutual exclusion & locks + deadlock).
- **2026-07-16** — **Lesson 02 quizzed** (spaced recall, 10 open-ended Qs, integrative with L01 per the carried-over quiz rule) → graded to [grades/02-shared-state-races.md](grades/02-shared-state-races.md). **Overall Solid: 5 Strong / 5 Partial / 0 Weak.** Strong on race legs+fixes, lost-update walk & concurrency-not-parallelism, torn-read hardware truth, 4 race shapes + why atomics can't cover multi-field, and race≠deadlock (resolves an L01 gap). **Recurring pattern firmly confirmed again** (L01 + system_design): correct conclusion fast, under-explains the consequence/"so what" half — cost a grade on Q3/Q4/Q8/Q9/Q10. Re-drill before/within L03: (1) precise critical-section definition (shared + mutual-exclusion, not "any non-atomic code"); (2) SILENT corruption as the headline race danger; (3) confinement = often-best fix + full buys/costs incl. deadlock as a lock cost; (4) coarse-vs-fine lock granularity / serialize-the-whole-structure trap; (5) block-on-lock → deschedule → 2 context switches cost model (L01 carry-over). **Next: write + teach Lesson 03 (mutual exclusion & locks + deadlock)** — takes fix-landscape row 1 and builds it.
- **2026-07-16** — **Lesson 03 written + taught.** `study/03-mutual-exclusion-locks.md` v1.0, full depth + Sources appendix ([S1] MS Learn `lock` statement, [S2] sync-primitives overview, [S3] Coffman deadlock conditions — all pulled/verified live this run). Covers: lock = mutual exclusion (acquire/release, at-most-one-holder, safety vs liveness, reentrancy); cost model (blocking = deschedule + ~2 context switches, tied to L01; vs spinlock = busy-wait; when each); lock granularity (coarse bottleneck vs striping, "start coarse, split only where a profiler shows contention"); 5 lock design rules (private lock with its data / same lock for same data / hold briefly / never call foreign code under lock / lock reads too) — the SRP/encapsulation half the round grades; deadlock (liveness fail, two-lock opposite-order walk + diagram); **4 Coffman conditions** + break-any-one; fixes (lock ordering default, TryEnter/timeout, coarsen) w/ tradeoffs; livelock vs starvation vs deadlock vs race. **Deliberately targets the 5 L02 re-drill gaps.** Demo `03-locks` added to harness: locked counter **20/20 exact**; opposite-order **deadlock detected via CountdownEvent watchdog timeout**, consistent-order completes cleanly (verified running). **Next session:** quiz Lesson 03 (spaced recall, integrative w/ L01–L02), then write + teach Lesson 04 (memory model & visibility).
- **2026-07-19** — **Lesson 03 quizzed** (spaced recall, 10 open-ended Qs, integrative with L01–L02) → graded to [grades/03-mutual-exclusion-locks.md](grades/03-mutual-exclusion-locks.md). **Overall Solid: 5 Strong / 5 Partial / 0 Weak.** Strong on blocking-vs-spinlock cost model (~2 ctx switches + single-core disaster + hybrid), the 5 lock design rules + torn-read→R/W lock, deadlock definition + two-lock walk, the four Coffman conditions + per-condition fixes + by-construction lock ordering, and reentrancy. **Milestone: the silent-corruption-vs-frozen contrast (long-standing L02 recurring gap) was led unprompted** — that specific gap looks resolved. Recurring "conclusion-fast / answers-only-part-of-a-multi-part-Q" pattern still live (skipped sub-parts on Q3/Q4, defaulted to perf framing on Q10 until pushed); per user directive I pressed every missed sub-part. Re-drill before L04: (1) precise safety/liveness defs; (2) deadlock ≠ contention; (3) starvation (+ livelock as its special case); (4) cache stampede / single-flight (per-key lock or in-progress future, not a global coarse lock). Sharp self-driven questions this session (block-vs-spin pseudocode, jitter probabilistic vs ordering deterministic, striping resize-orphan, compute idempotency). **Next: write + teach Lesson 04 (memory model & visibility).**
- **2026-07-20** — **Lesson 04 written + taught.** `study/04-memory-model-visibility.md` v1.0, full depth + Sources appendix ([S1] volatile keyword, [S2] Thread.MemoryBarrier, [S3] Interlocked class, [S4] Interlocked.CompareExchange — all pulled/verified live this run). Covers: the second hazard (visibility/ordering, distinct from L02's race) via the 3 reordering layers (compiler/JIT hoisting, CPU out-of-order/store buffers, per-core caches); the stop-flag-that-never-stops; the **memory model** + **happens-before** (writes published + ordered only across a happens-before edge); `volatile` = visibility + ordering of ONE field, with the sharp edges (NOT atomic RMW → does not fix `count++`; no multi-field/total order; no double/long); **memory barrier/fence** as the underlying primitive; **Interlocked** (Increment/Add/Exchange) + **CompareExchange/CAS** with the optimistic **retry loop** and the **ABA** preview (→ L07); and the unifier — **a lock = mutual exclusion + memory barrier, so visibility comes free** (why you rarely hand-roll volatile/barriers). Tool-ladder table (volatile / Interlocked / lock) + senior one-liner. Demo `04-memory` added to harness: plain `++` **0/20**, `Interlocked.Increment` **20/20**, **volatile-`++` 0/20** (visibility≠atomicity, the headline); volatile stop-flag stops promptly, non-volatile run under a watchdog with an honest JIT/build-dependence note (stopped this run in Debug). **Next session:** quiz Lesson 04 (spaced recall, integrative w/ L01–L03) — open with the 1-min refresher on the 4 L03 re-drill items (safety/liveness, deadlock≠contention, starvation/livelock, cache stampede/single-flight) — then write + teach Lesson 05 (condition synchronization).
- **2026-07-21** — **Lesson 04 quizzed** (spaced recall, 10 open-ended Qs, integrative with L01–L03; opened with the 1-min L03 re-drill refresher as planned) → graded to [grades/04-memory-model-visibility.md](grades/04-memory-model-visibility.md). **Overall Solid: 7 Strong / 3 Partial / 0 Weak** — the strongest quiz yet. Strong on: what volatile does/doesn't (incl. the `count++` walk), memory barriers + how volatile/Interlocked/lock build on them, CAS signature + retry loop + optimistic-commit + ABA, the lock-gives-visibility-free unifier + HB edge, the design tool-selection (counter/config-ref/stats), and the closing decision ladder. **Exceptional self-driven follow-ups again** (his signature strength): two-CAS multi-location loop (→ observer-half-state + retry-double-apply), and "how is Interlocked different from a spinlock?" (→ the real answer is holds-nothing/lock-free progress, not cost). Partial gaps: (1) **3-layer taxonomy muddled** — merged compiler/JIT + CPU reordering, promoted hoisting to a co-equal layer (it's a sub-case of compiler/JIT); (2) **happens-before defined as program order** — must stress it's *established by a synchronizing action*, not mere code/wall-clock order; (3) **Debug-won't-hang reason wrong** — said "single thread"; it's *Debug disables JIT hoisting*. Recurring "conclusion-fast, so-what-half-implicit" still surfaced (didn't state visibility≠atomicity aloud on Q8; missed the "passing run ≠ proof of correctness" half on Q9) — pressed each per standing directive. **Next: write + teach Lesson 05 (condition synchronization — producer/consumer, async logger).** Open its quiz with a 30-sec touch on the 3 L04 re-drill items above.
- **2026-07-21** — **Lesson 05 written + taught.** `study/05-condition-synchronization.md` v1.0, full depth + Sources appendix ([S1] Monitor.Wait, [S2] Monitor.Pulse, [S3] BlockingCollection\<T\>, [S4] BlockingCollection overview — all pulled/verified live this run; plus [S7] the reported Databricks logger Q). Covers: the missing verb (**wait FOR a condition**, vs L02–04's *protect*); busy-wait anti-pattern → L03 single-core disaster; **condition variable** + wait/signal/broadcast; the **atomic release-and-sleep** property + **lost-wakeup** if non-atomic; canonical waiter/signaler skeleton (check condition under the lock; CV **remembers nothing** → shared state = truth); the golden rule **`while` not `if`** with all three reasons (**Mesa semantics** [S2] 'not necessarily the pulsed thread reacquires the lock' + spurious + broadcast); **signal vs broadcast** + **thundering herd** + the two-CV fix; the **bounded producer–consumer** design (notFull/notEmpty, why `signal` not `broadcast`) + **backpressure** (unbounded → OOM) with SRP/cohesion framing; **async logger** (one writer/many producers, **batching** amortizes syscalls, bounded=backpressure vs OOM, **poison-pill/`CompleteAdding`** graceful shutdown, durability deferred → L10); language note (Monitor Wait/Pulse/PulseAll + BlockingCollection\<T\>, Channel\<T\> for async). **No watch-it-happen demo** (per the L02/03/04/09 harness scoping) — offered a runnable `BlockingCollection<T>` logger on request. **Next session:** quiz Lesson 05 (spaced recall, integrative w/ L01–04) — open with a 30-sec touch on the 3 L04 re-drill items — then write + teach Lesson 06 (higher-level primitives: semaphore, RW-lock, latch, barrier).
- **2026-07-22** — **Lesson 06 written + taught.** `study/06-higher-level-primitives.md` v1.0, full depth + Sources appendix ([S1] SemaphoreSlim, [S2] ReaderWriterLockSlim, [S3] ManualResetEvent, [S4] CountdownEvent, [S5] Barrier, [S6] Semaphore/SemaphoreSlim conceptual — all pulled/verified live this run from MS Learn). Frames the 5 primitives as **crystallized lock+condition-variable patterns** (Part 0 drawer table; Part 6 "built-from-lock+CV" recipes). Covers: **semaphore** (permit counter, acquire/release = P/V = Wait/Release; the headline **binary-semaphore ≠ mutex** — no owner/thread-affinity, any thread releases, not reentrant, extra release silently admits two [S6]; throttle + resource-pool uses w/ `finally`-release; L05 bounded queue rebuilt as **two semaphores + a mutex**; no-FIFO + spin-then-block [S1][S6]); **read-write lock** (readers **xor** writer invariant; the two conditions to beat a plain lock — reads dominate AND critical sections long enough; **writer starvation** + the favor-writers fairness policy [S2]; upgradeable read to avoid the double-upgrade deadlock; →L07 striping / →L09 cache; SynchronizedCache SRP); **events/latches** (manual-reset = gate, wakes **all**, stays open vs auto-reset = turnstile, wakes **one**, re-closes [S3]; start-signal); **countdown latch** (fork/join, one-shot, separate coordinator — the same primitive as the L03 deadlock-demo watchdog [S4]); **barrier** (N peers **rendezvous cyclically** each phase, SignalAndWait + post-phase action [S5]); countdown-vs-barrier contrast table; decision table; consolidated C# language-note table. **No watch-it-happen demo** (per L02/03/04/09 scoping) — offered a runnable `SemaphoreSlim` throttle / RW-cache on request. Threads back to L02 (races), L03 (lock/contention/granularity/starvation, hybrid spin), L04 (volatile/Interlocked as the tiny-read alternative), L05 (CV, signal-vs-broadcast, backpressure). **Next session:** quiz Lesson 06 (spaced recall, integrative w/ L01–05) → grade to grades/06, then write + teach Lesson 07 (concurrent data structures: thread-safe queue/map, lock striping/sharding, lock-free/CAS + ABA).

