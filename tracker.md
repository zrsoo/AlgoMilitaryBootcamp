# Retry Queue / Spaced Repetition Tracker

Cadence: **D+3, D+7, D+14**. Each rep is a blind, timed re-solve from a blank file (editorial closed, notes closed).

**Failure rule:** if you fail (couldn't solve cleanly in time budget, or needed >150% of original time at D+3/D+7) — move the row to "Failed reps", then re-add it to Active with today as the new first-solve date.

**Status values:** `scheduled` → `D+3 ✓` → `D+7 ✓` → `done`, or `failed-reset`.

Date format: `YYYY-MM-DD` (sorts lexically — grep today's date to find what's due).

---

## Active queue

| LC  | Title          | Pattern         | First solve | D+3        | D+7        | D+14       | Status    | Notes |
|-----|----------------|-----------------|-------------|------------|------------|------------|-----------|-------|
| 49  | Group Anagrams              | Arrays/Hashing  | 2026-05-23  | 2026-05-26 ✓ | 2026-05-30 ✓ | 2026-06-06 | D+7 ✓ | D+7: solved cleanly with count-array key (`new string(char[26])`) — where `char[26]` is a frequency array of the strings. faster than sort, O(N·K) vs O(N·K log K). |
| 560 | Subarray Sum Equals K        | Prefix Sum / Hashing | 2026-05-23 | 2026-05-26 ✓ | 2026-05-30 ✓ | 2026-06-06 | D+7 ✓ | Prefix sum + hashmap of `prefixSum → count`. Seed `{0:1}` for subarrays starting at index 0. Lookup `psum - k` BEFORE inserting current `psum`. Need counts (not a set) because negatives/zeros make prefix sums recur. Sliding window doesn't work due to negatives. Used `long` for psum to dodge overflow. |
| 128 | Longest Consecutive Sequence | Arrays/Hashing  | 2026-05-25  | 2026-05-28 ✓ | 2026-06-01 ✓ | 2026-06-08 | D+7 ✓ | D+7: solved cleanly. **Remember to iterate the set, not the array** — same O(n) asymptotically (dupes in the array are just skipped by the `n-1 ∈ set` head check), but iterating the set skips duplicate work outright and is the cleaner idiom. Canonical pattern: dump into `HashSet<int>`, then only start a walk when `n-1` is absent (sequence head) — keeps it O(n) instead of O(n²). |
| 974 | Subarray Sums Divisible by K | Prefix Sum / Hashing | 2026-05-25 | 2026-05-28 ✓ | 2026-06-01 ✓ | 2026-06-08 | D+7 ✓ | Variant of LC 560: count prefix-sum *remainders* mod k instead of raw sums. Key gotcha: C# `%` can return negative for negative dividends — normalize with `((psum % k) + k) % k`. Seed `{0:1}` so prefixes that are themselves divisible count. For each index, add `count[r]` to answer (pairs of equal remainder ⇒ difference divisible by k), then increment `count[r]`. |
| 15  | 3Sum                         | Two Pointers / Sort | 2026-05-26 | 2026-05-30 ✓ | 2026-06-02 ✓ | 2026-06-09 | D+7 ✓ | **Needed editorial for dedup pattern.** Sort + outer fixes `nums[i]`, inner two-pointer finds pairs summing to `-nums[i]`. Three dedup points: (1) outer skip `nums[i] == nums[i-1]` (i>0); (2) after a hit, while `nums[l] == nums[l+1]` advance l; (3) mirror for r. Early exit on `nums[i] > 0`. Template fragment for 3Sum Closest, 4Sum, kSum. |
| 75  | Sort Colors                  | Two Pointers / Dutch National Flag | 2026-05-26 | 2026-05-30 ✓ | 2026-06-02 ✓ | 2026-06-09 | D+7 ✓ | **Needed editorial.** One-pass in-place three-way partition: `low/mid/high`. Invariant: `[0..low-1]=0s`, `[low..mid-1]=1s`, `[mid..high]=unknown`, `[high+1..n-1]=2s`. Key asymmetry: 0-swap advances `mid` (swapped-in value was a confirmed 1), 2-swap does NOT advance `mid` (swapped-in value from `high` is still unknown). Loop `while mid <= high`. |
| 42  | Trapping Rain Water          | Prefix Max / Suffix Max (→ retry as Two Pointers) | 2026-05-27 | 2026-05-31 ✓ | 2026-06-03 ✓ | 2026-06-10 | D+7 ✓ | **Solved with O(n) space** using `leftMax[]` + `rightMax[]` arrays, then `min(L,R) - h[i]` per index. **Retry goal: O(1) space two-pointer version** — converge `l`/`r` inward, keep running `leftMax`/`rightMax` scalars, commit water on whichever side has the smaller running max (that side's bound is finalized). Same shorter-wall bottleneck argument as LC 11. |
| 287 | Find the Duplicate Number    | Fast & Slow Pointers / Floyd's Cycle Detection | 2026-05-27 | 2026-05-30 ✓ | 2026-06-03 ✓ | 2026-06-10 | D+7 ✓ | **Needed hint to see the pattern.** Treat array as implicit linked list: `next(i) = nums[i]`. With `n+1` values in `[1,n]` two indices share a successor ⇒ guaranteed cycle, and the duplicate IS the cycle entrance. Two phases: (1) tortoise/hare until meet inside cycle (`slow=nums[slow]`, `fast=nums[nums[fast]]`, `do/while`); (2) reset one to `0`, advance both by 1 until meet again — that index is the answer. Math: `a ≡ -b (mod cycleLen)`. Reusable for LC 141/142/202/457. |
| 525 | Contiguous Array             | Prefix Sum / Hashing | 2026-05-27 | 2026-05-31 ✓ | 2026-06-03 ✓ | 2026-06-10 | D+7 ✓ | Map `0 → -1` trick: treat 0 as -1, prefix sum equal at two indices ⇒ balanced subarray between them. Store **first index** per prefix sum (want max length, not count). **Miss:** didn't catch the "same count" case — when `sum == 0` the whole prefix `[0..i]` is balanced; handled by either seeding `{0: -1}` or special-casing `sum == 0 → maxLength = i + 1`. Sibling of LC 560/974 but tracks first-seen index instead of counts. |
| 424 | Longest Repeating Character Replacement | Sliding Window | 2026-05-29 | 2026-06-01 ✓ | 2026-06-05 | 2026-06-12 | D+3 ✓ | Sliding window + `count[26]` + running `maxFreq`. Window is valid iff `windowLen - maxFreq <= k` (replacements needed = everything that isn't the most common char). Key trick: don't recompute `maxFreq` on shrink — let it go stale. Stale value is an upper bound, window just shifts (not shrinks) until a genuinely larger window appears, which can only happen when the true `maxFreq` actually grows. Use `if` not `while` on the shrink → each char in/out O(1) ⇒ O(n). First attempt rebuilt `count` from scratch on every invalid window → O(n²) and corrupted counts when I forgot to decrement. |
| 567 | Permutation in String | Sliding Window (fixed size) | 2026-05-29 | 2026-06-01 ✓ | 2026-06-05 | 2026-06-12 | D+3 ✓ | Found O(n·m) brute force fast (rebuild freq array per window), but **didn't find optimal approach on my own** — missed the rolling-frequency patch (one in, one out as window slides). Build `freqA`/`freqB` once for first window, then on each step decrement leaving char and increment entering char; compare `freqA == freqB`. C# perf gotcha: use `arr.AsSpan().SequenceEqual(other)` (vectorized intrinsic, no allocation) instead of `Enumerable.SequenceEqual` (allocates enumerators, virtual dispatch). Don't use `Substring` in hot loop. Same template as LC 438. |
| 76  | Minimum Window Substring | Sliding Window (variable size) | 2026-06-01 ✓ | 2026-06-01 ✓ | 2026-06-08 | 2026-06-15 | D+3 ✓ | Re-solved cleanly today (compressed first-solve + D+3 — skipped reset penalty). Two arrays `need[128]` / `have[128]` plus a `satisfied` counter = # of distinct chars where `have[c] >= need[c]`; window valid iff `satisfied == required` (= distinct chars in t). Expand r, on `have[c] == need[c]` increment satisfied. While valid, record window and shrink from l: decrement have, on `have[c] < need[c]` decrement satisfied. `while` (not `if`) on shrink because we want the MINIMUM. Guard threshold checks with `need[c] > 0` to ignore chars not in t. Track `(bestL, bestLen)` not the substring itself — avoids per-shrink `Substring` allocation. O(\|s\| + \|t\|). |
| 239 | Sliding Window Maximum | Sliding Window + Monotonic Deque | 2026-05-31 | 2026-06-03 ✓ | 2026-06-07 | 2026-06-14 | D+3 ✓ | **Only found brute force without editorial.** First attempt used `SortedList` for an O(n·k) sort-of-solution that TLE-borderlines (array-backed ⇒ O(k) insert/remove in the middle, plus boxing). Optimal is monotonic-decreasing deque of **indices**: on each step (1) evict front if it slid out (`d.First <= r - k`), (2) pop dominated tail (`nums[d.Last] <= nums[r]`), (3) push `r`, (4) once `r >= k-1` record `nums[d.First]`. Each index enters/leaves once ⇒ amortized O(1) per step ⇒ O(n) total. Use a single-pointer loop (`for r in 0..n`) instead of two pointers — kills the awkward "advance then bounds-check" branch. `LinkedList<int>` works but allocates per node; `int[n]` head/tail deque is faster (ring of `int[k]` is tightest but uglier with mod arithmetic). |
| 20  | Valid Parentheses | Stack | 2026-05-31 | 2026-06-03 ✓ | 2026-06-07 | 2026-06-14 | D+3 ✓ | Solved optimally first try (O(n)/O(n)) — push the **expected closer** when seeing an opener so the pop-side check is a single `==`. First pass was correct but messy (nested `switch` on `Peek` with three near-duplicate branches comparing opener vs closer). **Retry (optional):** redo cold aiming for the cleaner push-closer form, or the dict-based `closer → opener` variant. |
| 41  | First Missing Positive | Arrays/Hashing — in-place index marking | 2026-05-31 | 2026-06-03 ✓ | 2026-06-07 | 2026-06-14 | D+3 ✓ | **Needed practice for the three-pass structure.** Answer always in `[1, n+1]` ⇒ use the array itself as a hash table via sign-flips at index `|v|-1`. Pass 1: neutralize out-of-range values (`<=0` or `>n`) to `1`, tracking whether `1` was present (short-circuit return if not). Pass 2: for each `v = |nums[i]|` in `[1,n]`, mark `nums[v-1] = -|nums[v-1]|` (guard with `if (nums[idx] < 0) continue` to avoid un-flipping duplicates). Pass 3: first index with positive value ⇒ answer `i+1`, else `n+1`. Indexing footguns: always `Math.Abs(nums[i]) - 1`; neutralize `>n` in pass 1 or pass 2 indexes OOB. |
| 394 | Decode String | Stack — two stacks (count + prefix) | 2026-06-01  | 2026-06-04 | 2026-06-08 | 2026-06-15 | scheduled | **Couldn't solve cleanly first try.** Attempted right-to-left single-stack approach — got tangled mixing outer-scope letters with inner-scope results in one `temp` builder (trailing letters after a `]` like `ef` got pulled into the repetition). Canonical pattern: **left-to-right with two stacks** (`Stack<int>` for counts, `Stack<StringBuilder>` for prefixes) + a `current` builder + digit accumulator `k = k*10 + (c-'0')`. On `[`: push k, push current, reset both. On `]`: pop count, pop prev, append `current` repeated count times to prev, current = prev. On letter: append to current. One pass, O(n·maxDepth) implicit, no `Math.Pow`, no reversal. |
| 84  | Largest Rectangle in Histogram | Stack — Monotonic Increasing (indices) | 2026-06-02 | 2026-06-05 | 2026-06-09 | 2026-06-16 | scheduled | **Only found O(n²) brute force on my own** Optimal: monotonic-increasing stack of **indices**. While `heights[i] < heights[stack.Peek()]`, pop — the popped bar's right boundary is `i`, left boundary is new top of stack (or `-1` if empty), width = `i - left - 1`. Sentinel `0` appended (or final flush loop) guarantees every bar gets popped. Each index pushed/popped at most once ⇒ O(n). Same family as LC 739 (next greater) — just "next smaller" + area instead of distance. Foundation for LC 85 Maximal Rectangle. |
| 85  | Maximal Rectangle | Stack — histogram reduction (LC 84 per row) | 2026-06-02 | 2026-06-05 | 2026-06-09 | 2026-06-16 | scheduled | **Couldn't find the approach without a hint.** Key reduction: for each row `r`, build `heights[c]` = number of consecutive `1`s in column `c` ending at row `r` (`heights[c] = matrix[r][c]=='1' ? heights[c]+1 : 0`, reset to 0 on `0` because the floor is broken). That row's histogram represents every rectangle whose bottom edge sits on row `r`. Run LC 84 largest-rectangle-in-histogram per row, take the max. Every rectangle in the grid has some bottom row ⇒ full coverage. O(n·m) time, O(m) space (single rolling `heights` array). Histogram subroutine: iterate `i` from 0 to `n` with `currentHeight = i==n ? 0 : hist[i]` sentinel to drain stack; on pop, width = `stack.Empty ? i : i - stack.Peek() - 1`, popped value `el` is the height ⇒ area = `el * width`. |
| 33  | Search in Rotated Sorted Array | Binary Search | 2026-06-02 | 2026-06-05 | 2026-06-09 | 2026-06-16 | scheduled | **Needed hint for the "which half is sorted" pivot.** Standard binary search loop, but at each `mid` one half is guaranteed sorted — detect with `nums[l] <= nums[mid]` (the `=` matters when the window shrinks to 1–2 elements). Then check if `target` lies in the sorted half's range (inclusive on the far end, exclusive on `mid` since `nums[mid] == target` is handled first): if yes, recurse into the sorted half; if no, it must be in the other (still-rotated) half. No duplicates in input — that's LC 81. O(log n). |
| 410 | Split Array Largest Sum | Binary Search on Answer | 2026-06-03 | 2026-06-06 | 2026-06-10 | 2026-06-17 | scheduled | **Needed hint to see "binary search on the answer" + feasibility check.** Search space is `[max(nums), sum(nums)]`. Greedy feasibility for candidate `mid`: walk array, start `count=1, sum=0`; if `sum + nums[i] > mid` close current subarray (`count++`, `sum = nums[i]`) else `sum += nums[i]`. If `count <= k` → `mid` is generous, shrink `r = mid`; else `l = mid + 1`. Converges to answer (`return l`). **Bug log:** initially cut on `sum >= mid` (lets subarray exceed `mid`), inverted comparison (`<` vs `<=` matters at boundary), forgot trailing subarray (start `count=1`, don't try to track it on cut). Same template as LC 875 Koko. |
| 435 | Non-overlapping Intervals | Intervals / Greedy | 2026-06-06 | 2026-06-09 | 2026-06-13 | 2026-06-20 | scheduled | **Couldn't solve without hints.** Sort by start, then greedy: keep a running `prevEnd`; on overlap (`prevEnd > intervals[i][0]`) drop one interval (`ans++`) and keep whichever ends earliest (`prevEnd = Math.Min(prevEnd, intervals[i][1])`) — keeping the earlier end leaves the most room for the rest. No overlap → advance `prevEnd`. Use strict `>` so touching intervals like `[1,2],[2,3]` don't count as overlapping. Min removals = total − max non-overlapping set (classic activity-selection). O(n log n). |
| 218 | The Skyline Problem | Sweep Line + Max-Heap (lazy deletion) | 2026-06-07 | 2026-06-10 | 2026-06-14 | 2026-06-21 | scheduled | **Couldn't solve.** Sweep line: split each building into start `(L,-H)` / end `(R,+H)` events; sort by x then encoded height (start-before-end, taller-start-first, shorter-end-first at equal x). Maintain current max active height; emit `[x, curMax]` whenever it changes. **Core block:** how to get the new max after a height goes inactive. C# `SortedDictionary.Last()` is LINQ → O(n) enumeration (no O(log n) max accessor) → secretly O(n²). Fix = max-heap (`PriorityQueue`, negate priority) + **lazy deletion**: ends only record a pending-removal count; purge retired elements only when they bubble to the top (`while pending[Peek()]>0 dequeue`). Seed heap with 0 for ground/descent. Each height pushed/popped once → amortized O(log n) → O(n log n). |
| 143 | Reorder List | Linked List — mid + reverse + weave | 2026-06-10 | 2026-06-13 | 2026-06-17 | 2026-06-24 | scheduled | **Solved on my own, but with O(n) space** (push all nodes to a stack, then re-link front→popped-back alternately). Needed templates for the O(1)-space version: (1) slow/fast to find middle, (2) 3-pointer in-place reversal of the back half, (3) weave-merge alternating nodes (stash both `next` links before rewiring, loop `while second.next != null` to keep tail null-terminated). The stack version is just a manual reverse of the back half. O(n) time both ways; O(1) space is the target. |
| 25  | Reverse Nodes in k-Group | Linked List — group reversal | 2026-06-10 | 2026-06-13 | 2026-06-17 | 2026-06-24 | scheduled | **Solved on my own with a stack** (push k node *copies*, pop them into the result to reverse). Time is amortized **O(n)** (every node pushed once / popped once — the nested loop is bounded by an external counter that resets), NOT O(n·k). Space is O(k) auxiliary, but it allocates n new nodes (copies every node instead of relinking). **Retry (optional):** in-place O(1)-space pointer reversal per group — reverse k links, reconnect prev-group tail → new head and group tail → next head; iterative or recursive. |
| 4   | Median of Two Sorted Arrays | Binary Search (partition / cut) | 2026-06-04 | 2026-06-07 | 2026-06-11 | 2026-06-18 | scheduled | **Got full editorial before first solve — didn't attempt cold.** Reframe: median = the cut point of a balanced ordered partition of the merged array. Pick `i ∈ [0, m]` = how many of A on the left; force `j = (m+n+1)/2 - i`. Always swap so A is the shorter array, else `j` can leave `[0, n]`. Valid iff `maxLeftA <= minRightB && maxLeftB <= minRightA`; use `±∞` sentinels for `i==0/m`, `j==0/n` to kill edge branches. Pivot: `maxLeftA > minRightB` ⇒ i too big (`hi=i-1`); `maxLeftB > minRightA` ⇒ i too small (`lo=i+1`). Answer: odd total → `max(maxLeftA, maxLeftB)`; even → avg of `max(lefts)` and `min(rights)` (remember `/ 2.0`). O(log min(m,n)), O(1). |
| 23  | Merge K Sorted Lists | Heap — k-way merge (min-heap of size k) | 2026-06-08 | 2026-06-11 | 2026-06-15 | 2026-06-22 | scheduled | **Needed a hint.** First attempt was the off-pattern "dump every node into the heap, then rebuild" — works but ignores that the lists are already sorted (heap grows to `N`, allocates `N` new nodes) → `O(N log N)`. Intended k-way merge: seed heap with the **head of each list** (size `k`), then repeatedly pop the smallest and push **its successor** — heap never exceeds `k`, splice the existing nodes instead of allocating. Use `int` priority (the node's val) so `ListNode` is never compared. Dummy head removes the `result == null` first-node special case. Last popped node has `next == null` so no explicit tail termination needed. `O(N log k)` time, `O(k)` space. D&C pairwise merge (LC 21 two at a time) is the alternative, also `O(N log k)`. |
| 295 | Find Median from Data Stream | Heap — two heaps (max-heap lower / min-heap upper) | 2026-06-09 | 2026-06-12 | 2026-06-16 | 2026-06-23 | failed-reset | **Failed first solve — needed full editorial.** Couldn't derive the two-heap split cold. Pattern: `lo` max-heap (smaller half) + `high` min-heap (larger half); keep every `lo` ≤ every `high` and sizes balanced with `lo` holding the extra when odd. Add via funnel: `EnqueueDequeue` onto `lo` then push that top into `high`, rebalance if `high.Count > lo.Count`. Median = `lo.Peek()` (odd) or average of both tops (even, `/ 2.0`). C# `PriorityQueue` is a min-heap → negate priority for `lo`. `EnqueueDequeue` does the push+pop in one op. `AddNum` O(log n), `FindMedian` O(1). Watch int-overflow on the even-case sum for extreme values. |
---

## Completed (passed all 3 reps)

| LC | Title | Pattern | Final pass date |
|----|-------|---------|-----------------|
| 206 | Reverse Linked List | Linked list — in-place iterative reversal (prev/curr/next) | 2026-06-09 (first-try clean, no reps) |
| 21 | Merge Two Sorted Lists | Linked list — merge with dummy head | 2026-06-09 (first-try clean, no reps) |

---

## Failed reps (reset history)

| LC | Title | Failed at | Failure date | Reason | New first-solve date |
|----|-------|-----------|--------------|--------|----------------------|
| 295 | Find Median from Data Stream | First solve | 2026-06-09 | Needed full editorial — couldn't derive the two-heap pattern cold | 2026-06-09 |
