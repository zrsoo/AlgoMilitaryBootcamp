# LC 981 — Time Based Key-Value Store
- **Pattern:** Design — per-key sorted list of `(timestamp, value)` + binary search for the floor (largest `timestamp <= query`; upper-bound then step one left)
- **Brute force:**
- **Optimized:**
- **Key insight:**
- **Edge cases I had to handle:**
- **Where I got stuck and for how long:**
- **Template fragments I reused:**
- **Would I solve this in 25 min cold next week? Y/N**
